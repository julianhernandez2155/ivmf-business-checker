# IVMF Business Checker — runtime dependencies
# Versions pinned for reproducibility. Update intentionally, run pytest after.

# Core HTTP + data validation
requests==2.33.0
pydantic==2.12.5

# Excel I/O
openpyxl==3.1.5

# Env-var loading
python-dotenv==1.2.2

# HTML parsing (direct-HTTP scrape fallback)
beautifulsoup4==4.13.4

# Web dashboard (Flask V2 entry point)
flask==3.1.1

# Native window for PyInstaller bundle
pywebview==6.1

# Note: development tooling (pytest, ruff, etc.) lives in requirements-dev.txt
