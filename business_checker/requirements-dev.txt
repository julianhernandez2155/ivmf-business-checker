# IVMF Business Checker — development dependencies
# Install with: pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Test framework
pytest==9.0.2

# Linter / formatter (config in pyproject.toml)
ruff>=0.5.0
