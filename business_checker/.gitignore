# Secrets — never commit
.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# SQLite cache (rebuilt per run, large)
cache/
*.db
*.db-journal

# Run output — large files, not useful in version control
Runs/

# V2 Flask temp uploads
.tmp_uploads/

# macOS
.DS_Store
