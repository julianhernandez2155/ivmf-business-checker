# Phase 00 Plan 04 additions: *.tsbuildinfo, pnpm-workspace.yaml scratch.
# Next.js build artifacts
.next/
out/

# Dependencies
node_modules/

# Drizzle scratch output (we never commit anything under here; introspect-only)
drizzle/.scratch/

# Turbo / Vercel
.turbo/
.vercel/

# Local env
.env
.env.local
.env*.local

# Editor
.vscode/
.idea/
*.swp

# TypeScript incremental build cache
*.tsbuildinfo

# pnpm auto-generated workspace scratch (allowBuilds prompt placeholder)
pnpm-workspace.yaml

# codegen artifacts are COMMITTED (not ignored) — drift gate compares against committed copy
# (i.e. do NOT add db/schema.ts or db/types.ts here)
